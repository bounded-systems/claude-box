---
description: Request a capability from concierge on demand, scoped to this room. The ONE sanctioned client command for this — do not reach the provider directly.
argument-hint: <capability> <ref>
---

Run `bun bellhop.ts $ARGUMENTS` and report back exactly what it prints: the checkout path on success, or the error message on failure. Do not attempt this any other way — bellhop is the ONLY sanctioned path to this capability.

