{
  "name": "claude-box",
  "version": "0.1.0",
  "private": true,
  "description": "Pinned, isolated, multi-account Claude runtime — OCI images published to GHCR on tagged releases.",
  "repository": "github:bounded-systems/claude-box",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version",
    "release:tag": "bash scripts/release-tag.sh"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9"
  }
}
